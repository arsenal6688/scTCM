[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sctcm"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "pandas>=1.4.0",
  "numpy>=1.21.0",
  "anndata>=0.8.0"
]
